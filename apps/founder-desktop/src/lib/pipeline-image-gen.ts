// Deprecated -- this module was removed when the brand logo step
// pivoted from Imagen REST to subscription-CLI / SVG. Kept as an
// empty placeholder because the D: drive mount can't delete files
// from the Linux side. Safe to delete manually on Windows.
export {};
