{
  "name": "@founder-os/handoff-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "echo test handoff-contract",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@founder-os/domain": "workspace:*",
    "@founder-os/artifacts-core": "workspace:*",
    "@founder-os/audit-contract": "workspace:*",
    "zod": "^3.23.8"
  }
}
