{
  "name": "@founder-os/handoff-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "echo test handoff-desktop",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@founder-os/handoff-contract": "workspace:*",
    "@founder-os/workspace-core": "workspace:*",
    "@founder-os/logger": "workspace:*",
    "@founder-os/domain": "workspace:*",
    "@founder-os/state": "workspace:*"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "@tauri-apps/api": "^2.5.0"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.5.0",
    "react": "^19.1.0"
  }
}
